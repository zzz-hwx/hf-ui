// 工具函数
// import * as utils from './libs/util/utils.js';

// 设置获取数据的函数
import { setUtils } from './libs/util/loadData.js';

// mixin
// import formMixin from './libs/mixins/form.js';	// 表单

const install = (Vue) => {
	
}

export default {
	install,
	
	setUtils,
	// ...utils,
	// formMixin,
}
